<div class="user_info">
  <a href="<%= user_path(current_user) %>">
    <%= link_to default, root_path %>
<!--	<%= gravatar_for current_user, :size => 30 %> -->
    <span class="user_name">
      <%= current_user.name %><br/>
	  <%= pluralize(current_user.microposts.count, "idea") %>
    </span>
  </a>
</div>