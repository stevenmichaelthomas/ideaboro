<div class = "fieldtitle">Additional thoughts (HTML is ok)</div>
<%= form_for([@micropost, @micropost.comments.build]) do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


